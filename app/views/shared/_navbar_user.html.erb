<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
  <div class="container-fluid">
    <%= link_to image_tag('favicon.png', :size=>"26x26", :class=>"mr-2"), root_path %>
    <%= link_to "iHerbs", root_path, :class=>"navbar-brand" %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item <%= yield(:home_active) %>">
          <%= link_to "Home", root_path, :class=>"nav-link"%>
        </li>
        <li class="nav-item <%= yield(:herb_active) %>">
          <%= link_to "Herbs", herbs_path, :class=>"nav-link"%>
        </li>
        <li class="nav-item <%= yield(:contact_active) %>">
          <%= link_to "Inquiry", contacts_path, :class=>"nav-link"%>
        </li>
      </ul>
      <ul class="navbar-nav">
        <li class="nav-item">
          <span class="nav-link">
          <% if current_user.image? %>
            <%= image_tag(current_user.image, :size=>"26x26", :class=>"rounded-circle") %>
          <% else %>
              <%= image_tag('https://images.pexels.com/photos/189349/pexels-photo-189349.jpeg?auto=compress&cs=tinysrgb&dpr=2&h=750&w=1260', :size=>"26x26", :class=>"rounded-circle") %>
          <% end %>

          <% if current_user.name? %>
            <%= current_user.name %>
          <% else %>
            <%= current_user.email %>
          <% end %>
          </span>
        </li>

        <li class="nav-item">
          <%= link_to destroy_user_session_path, :class=>"btn btn-danger ml-2 mt-1", method: :delete do %>
            Log Out
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>