<% provide(:page_title, "Inquiry Request") %>
<% provide(:contact_active, "active") %>
<div class="container">
  <div class="row justify-content-center" style="margin-top: 8vh">
    <div class="col-lg-12">
      <div class="card mb-2">
        <div class="card-body">
          <h3 class="py-3">Inquiry Form</h3>
          <%= render 'form', contact: @contact %>
        </div>
      </div>
    </div>
  </div>
</div>